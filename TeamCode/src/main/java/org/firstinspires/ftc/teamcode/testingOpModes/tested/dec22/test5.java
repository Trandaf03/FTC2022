package org.firstinspires.ftc.teamcode.testingOpModes.tested.dec22;

/**
 *
 * TEST SUCCESFULY
 *
 * */

/*
import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

import org.firstinspires.ftc.teamcode.testingOpModes.tested.dec22.test4_driveComponents;
import org.firstinspires.ftc.teamcode.utilities.driveUtilities.encoderUsing;
import org.firstinspires.ftc.teamcode.utilities.driveUtilities.powerBehavior;
import org.firstinspires.ftc.teamcode.utilities.driveUtilities.robotDirection;


@Autonomous(name = "AutoTestingByVelocity  AND PID", group = "testing")
public class test5 extends LinearOpMode {

    private test4_driveComponents drive = new test4_driveComponents();

    @Override
    public void runOpMode() throws InterruptedException {

        drive.driveInitialization(hardwareMap, robotDirection.ROBOT_DIRECTIONS.FORWARD, powerBehavior.ROBOT_BREAKING.BRAKE, encoderUsing.ENCODER_RUNNING_MODE.RUN_USING);
        waitForStart();
        if(opModeIsActive());

        drive.moveRobotByPID(50,0.5);

    }
}
*/